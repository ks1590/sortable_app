<% if @status == 'success' %>
  $("#records").empty();
  $("#records").append("<%= j(render("li", blog: @blog)) %>");
<%  elsif @status == 'fail' %>
  alert('error!');
<% end %>
  $("#calendar").fullCalendar(
    'renderEvent',
    $.parseJSON("<%=j render(@event, format: :json).html_safe %>"),true
  );  

$("#blog-modal").modal("hide");